// ondalıklı sayıyı tam sayıya dönüştürme

#include <stdio.h>

int main(int argc, char const *argv[]) {
    float n = 6.4461;
    printf("Ondalıklı sayı olan %f sayısının tam sayı değeri = %d\n", n, (int)n);

    return 0;
}